# Test fixtures

E2E fixture media lands here in a later task group: one clip of at most 30 seconds,
CC BY or CC0 only (no NC / ND). Record source URL, author, licence and the exact
trim range in this file before committing any media.

`.gitignore` ignores media everywhere except `tests/fixtures/*.mp4`.
